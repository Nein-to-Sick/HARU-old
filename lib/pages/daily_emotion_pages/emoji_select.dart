import 'package:flutter/material.dart';

class EmojiSelect extends StatelessWidget {
  const EmojiSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [
        Text('emoji'),
      ],
    );
  }
}
